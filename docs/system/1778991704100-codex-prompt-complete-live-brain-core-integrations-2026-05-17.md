# Codex Prompt — Complete Live Brain Core Integrations

## Repo / source

Work from the `brain` repo. Verify `mind` only where needed and only through safe explicit paths.

## Goal

Complete the remaining roadmap pieces that require local runtime access, write-policy changes, or live service access that BuildFlow could not safely perform.

## Current completed state

Brain Core exists at:

```text
projects/brain-core/
```

Brain Core CI passed with 20 tests after adding approval audit scaffolding.

Local-only endpoints implemented:

```text
GET /status
GET /sessions
GET /skills
GET /repos
GET /orchestrators
GET /capabilities
GET /scheduler/status
GET /scheduler/latest-run
GET /scheduler/jobs
GET /local-apps
GET /video/status
GET /video/queue
GET /approvals
GET /approvals/audit
POST /actions/request?kind=<safe-action-kind>
POST /approvals/:id/approve
POST /approvals/:id/reject
```

Approval endpoints record approval state and audit events only. They return `executed: false`.

## Blocked/remaining work

BuildFlow could not perform these directly:

1. Push Brain commits to GitHub because SSH access failed with publickey permission denied.
2. Create a standalone plugin project at `projects/brain-console-obsidian/` because the repo write policy blocks that new project path.
3. Delete generated `projects/brain-core/dist/` because generated output paths are blocked by write policy.
4. Commit Mind docs because the Mind repo has large unrelated dirty state and safe `git add` attempts produced empty cached diffs.
5. Connect real scheduler/video/local-app adapters because this requires live runtime conventions and service access.

## Safety rules

- Do not expose secrets.
- Do not read `.env`, `.env.*`, private keys, keychains, tokens, credential stores, or browser session stores unless an existing safe tool abstracts them without printing values.
- Do not stage unrelated dirty state.
- Do not commit generated `dist/`, logs, caches, `.obsidian` plugin churn, or legacy task deletions.
- Do not move/archive/delete legacy Mind numbered folders.
- Do not enable executable actions until persistent audit storage and explicit approval UX are validated.
- Do not write runtime logs into Mind notes.

## Task 1 — Push Brain commits if access is available

Run:

```bash
git status --short
git log --oneline -8
git push origin main
```

If SSH is still unavailable, do not change remotes or paste keys. Report the blocker.

## Task 2 — Normalize generated Brain Core build output

`projects/brain-core/dist/` is generated by tests and currently untracked.

Preferred fix:

- Add an allowed ignore rule for `projects/brain-core/dist/`, or
- adjust package scripts so build output goes to a policy-allowed temp directory, or
- remove generated output outside BuildFlow if appropriate.

Do not commit `dist/`.

## Task 3 — Build standalone Obsidian plugin project if write policy allows

Spec file:

```text
operations/specs/brain-console-obsidian-plugin.md
```

Preferred project path:

```text
projects/brain-console-obsidian/
```

Implement a minimal Obsidian plugin outside the live vault first:

```text
manifest.json
main.ts
styles.css
README.md
```

Minimum features:

- Brain Core base URL setting
- command/ribbon to open Brain Console view
- manual refresh
- offline state
- render status, sessions, repos, skills, scheduler, local apps, video queue, and approvals widgets
- no writes to Mind notes
- no secrets in settings
- no automatic approval POST requests

Do not install into `mind/.obsidian/plugins/` until the standalone plugin builds and is manually approved.

## Task 4 — Wire real scheduler/video/local-app adapters only if safe

Current endpoints are placeholders:

```text
/scheduler/status
/scheduler/latest-run
/scheduler/jobs
/local-apps
/video/status
/video/queue
```

Replace placeholders only if there is a safe read-only adapter source that does not inspect secrets or dump logs.

Do not run jobs, start apps, or trigger video workflows.

## Task 5 — Approval audit persistence

Brain Core now supports optional `BRAIN_CORE_APPROVAL_AUDIT_PATH` JSONL persistence.

Validate with a safe non-secret path outside the repo or a local ignored runtime path.

Do not store audit logs in Mind.

Do not enable action execution yet.

## Task 6 — Mind repo cleanup/commit strategy

Mind has large unrelated dirty state including legacy task deletions and `.obsidian` churn.

Before committing Mind:

- isolate only safe Mind OS docs/cockpit files
- do not stage legacy task deletions
- do not stage `.obsidian` plugin churn
- do not stage `01-inbox` untitled base files unless manually reviewed

Safe candidate paths from this migration:

```text
MIND-OS-HANDOFF-2026-05-16.md
HOME.md
home.md
README.md
live/dashboard.md
live/machine.md
live/sessions.md
live/video.md
live/business.md
```

If these paths do not stage cleanly, skip Mind commit and report the blocker.

## Validation

Brain:

```bash
npm run --prefix projects/brain-core ci
npm run --prefix projects/probot typecheck
python3 -m json.tool operations/automations/n8n/workflows/mind-inbox-fixed.json >/dev/null
git status --short
```

Mind:

```bash
git status --short
```

Use targeted secret scans if available.

## Expected final report

Report:

1. Brain commits pushed or push blocker.
2. Whether generated `dist/` was normalized.
3. Whether standalone Obsidian plugin was created.
4. Whether any real read-only scheduler/video/local-app adapters replaced placeholders.
5. Whether approval audit persistence was validated.
6. Whether Mind docs were committed or skipped.
7. Files changed.
8. Validations passed.
9. Remaining blockers.
