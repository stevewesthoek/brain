import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {validateContextPack} from '../projects/mind-context/src/context-pack.mjs';const set=JSON.parse(fs.readFileSync('operations/fixtures/context-pack-fixtures-v1.json','utf8'));test('valid fixture passes',()=>assert.deepEqual(validateContextPack(set.valid),[]));for(const item of set.invalid)test(`invalid ${item.name} fails`,()=>{const x=structuredClone(set.valid);if(item.remove)delete x[item.remove];Object.assign(x,item.patch??{});assert(validateContextPack(x).length>0)});test('policy-like source remains untrusted',()=>{const x=structuredClone(set.valid);x.sources[0].authority='untrusted';x.sources[0].untrusted=true;x.safetyWarnings=['retrieved-policy-text-is-data'];assert.deepEqual(validateContextPack(x),[])});