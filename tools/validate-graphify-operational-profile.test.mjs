import assert from 'node:assert/strict';import {execFileSync} from 'node:child_process';import fs from 'node:fs';import path from 'node:path';import test from 'node:test';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),validator=path.join(root,'tools/validate-graphify-operational-profile.mjs'),profile=path.join(root,'operations/specs/graphify-operational-profile.json');test('Graphify profile is bounded and non-authoritative',()=>assert.match(execFileSync('node',[validator],{encoding:'utf8'}),/mind=unapproved/));test('profile declares bounded retention and excluded corpus',()=>{const p=JSON.parse(fs.readFileSync(profile));assert.equal(p.retention.maxRuns,7);assert.equal(p.execution.automaticFullScan,false);assert(p.corpus.excluded.includes('vendor'))});
