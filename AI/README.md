# AI

Central place for AI configs, agents, and prompt libraries.

- `agents.md` – global reusable agent definitions (roles + constraints)
- `prompts/` – prompt templates grouped by topic (web design, SaaS, theology, etc.)
- `providers/` – provider-specific notes (Gemini, Codex, Claude…)
- `skills/` – skill descriptions that can be reused across tools