# Legacy Runtime Notes

This folder is intentionally minimal.

## Rule

- old duplicated runtime notes were removed to prevent drift
- do not recreate personal or business copies here
- prefer `personal/`, `organisations/`, `projects/`, `ai/`, `operations/`, and the main files in `runtime/openclaw/`
