# 2026-03-24

- Standardized the OpenClaw workspace around `runtime/openclaw/`.
- Kept the repo root clean and used linked canonical folders for broader Brain access.
- Full Brain migration completed: repo synced to d6559be, all workspace skills updated to lowercase canonical paths.
- Skills migrated: brain, tweets, reply, comment (removed all references to PROBOT.md, RESTRICTED_REPOS.md, AI/twitter*, Personal/, Business/, Organisation/).
- Calendar and schedule skills were already clean (no Brain path references).
- ProBot confirmed as canonical identity.
- Created `active-skills/` directory in Brain repo for OpenClaw-specific skills organized by domain.
- Moved all 6 workspace skills into Brain repo: brain, google/calendar, x/comment, x/reply, x/schedule, x/tweets.
- Renamed skill identifiers: calendar→google_calendar, comment→x_comment, reply→x_reply, schedule→x_schedule, tweets→x_tweets.
- Replaced workspace skill directories with flat namespaced symlinks into Brain repo.
- Set repo-local Git identity: ProBot <info@prochat.tools>.
- Updated TOOLS.md, README.md, AGENTS.md with new skill structure documentation.
- Updated workspace root files (AGENTS.md, SOUL.md, USER.md, IDENTITY.md, TOOLS.md) to point to Brain bootstrap — Brain context now auto-loaded every session.
- Removed brain skill (obsolete — Brain context always available via workspace root instructions).
- Removed BOOTSTRAP.md (initial setup complete).
- Final active skills: google-calendar, x-comment, x-reply, x-schedule, x-tweets.


## Correction — skills model update (same day)

The earlier entries describing flat namespaced symlinks in `~/.openclaw/workspace/skills/` are superseded.
The final model is:
- `workspace/skills/` is empty
- OpenClaw loads skills via `extraDirs` in `~/.openclaw/openclaw.json` directly from Brain paths
- Shared skills: `brain/ai/skills/` (notebooklm, ui-ux-pro-max, web-design)
- Active skills: `brain/runtime/openclaw/active-skills/x/`, `brain/runtime/openclaw/active-skills/google/`
