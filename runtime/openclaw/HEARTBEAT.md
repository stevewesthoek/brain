# Heartbeat

- Check `tasks.md` if it exists.
- Prefer canonical docs before runtime notes.
- Update `MEMORY.md` only for durable facts.
- Move stable facts into canonical folders when a clear home exists.
