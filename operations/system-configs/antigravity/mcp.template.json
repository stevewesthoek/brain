{
  "servers": {
    "stitch": {
      "serverUrl": "https://stitch.googleapis.com/mcp",
      "headers": {
        "Authorization": "Bearer <YOUR_ACCESS_TOKEN>",
        "X-Goog-User-Project": "<YOUR_GCP_PROJECT_ID>"
      }
    }
  },
  "inputs": [],
  "notes": {
    "notebooklm": "NotebookLM v0.3.4 CLI is not an MCP server — use /notebooklm skill directly for research, content generation, batch export"
  }
}
