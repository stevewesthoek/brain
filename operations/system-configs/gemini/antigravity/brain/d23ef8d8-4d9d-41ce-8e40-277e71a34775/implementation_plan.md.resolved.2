# Spacing Optimization Plan

## Goal
Reduce vertical whitespace between sections on the Builders Bootcamp page by 25% and ensure uniform, even spacing throughout the page.

## Proposed Changes

### Builders Bootcamp Page
#### [MODIFY] [page.tsx](file:///Users/Office/Repos/Clients/JC Citadel/jpv-bootcamp/src/app/builders-bootcamp/page.tsx)

1.  **Hero Section**: Reduce padding from `pt-28 sm:pt-24 pb-12` to `pt-21 sm:pt-18 pb-9`.
2.  **What to Expect / Why Exhibit? Section**: Adjust from `py-24` to `py-24 sm:py-36` to match the new standardized spacing.
3.  **The Vision Section**: Reduce from `py-32 sm:py-48` to `py-24 sm:py-36`.
4.  **Why This Matters Section**: Reduce from `py-32 sm:py-48` to `py-24 sm:py-36`.
5.  **Speakers Section**: Reduce from `py-32 sm:py-48` to `py-24 sm:py-36`.
6.  **Tickets Section**: Reduce from `py-32 sm:py-48` to `py-24 sm:py-36`.

## Verification Plan
- Visually inspect the page to ensure sections look more compact but still well-balanced.
- Verify that the gap between each section is identical across the page.
