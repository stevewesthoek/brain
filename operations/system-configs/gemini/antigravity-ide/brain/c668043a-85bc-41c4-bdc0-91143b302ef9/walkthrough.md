# Repository Sync Walkthrough

I have successfully synced the repository to the `main` branch.

## Changes Made
- Moved `brain-configs-link.sh` to the `Scripts/` directory.
- Added a new `README.md` file.

## Verification
The following git commands were executed:
1. `git add .`
2. `git commit -m "Move brain-configs-link.sh to Scripts/ and add README.md"`
3. `git push origin main`

The push was confirmed with the following result:
`   26ce0ad..77795ab  main -> main`
