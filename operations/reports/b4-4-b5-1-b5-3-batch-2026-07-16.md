# B4.4 and B5.1-B5.3 Batch Summary

**Date:** 2026-07-16  
**Status:** complete  
**Scope:** Brain-only canonical status/view and controlled proposal application batch

## Completed tasks

- B4.4 - Expose one status view
- B5.1 - Align proposal and approval schemas
- B5.2 - Bind executor to capability state
- B5.3 - Complete fixture write/rollback loop

## Canonical evidence

### B4.4

The status view is sourced from a single manifest and generator path:

- `operations/specs/infinite-brain-capabilities.json`
- `tools/generate-infinite-brain-capability-status.mjs`
- `operations/runbooks/infinite-brain-roadmap-status.md`

The generator renders the capability table from the manifest, preserves the
human-authored notes section outside generated markers, and detects manual
edits inside the generated block. CLI and fixture usage share the same
manifest/status core, so the Markdown status view, CLI surface, and fixture
validation agree on capability IDs and states.

Validation evidence:

- `node --test tools/generate-infinite-brain-capability-status.test.mjs`

### B5.1

The exact-scope approval contract is implemented in:

- `projects/brain-core/src/adapters/infinite-brain-exact-scope-approval.ts`
- `projects/brain-core/src/tests/infinite-brain-exact-scope-approval.test.ts`

The validator requires:

- exact repository-relative paths;
- explicit allowed line sections;
- before hashes;
- source commit;
- expiry;
- source references;
- idempotency key;
- rollback requirements;
- trusted approval identity.

The implementation fails closed on traversal, globbing, stale hashes, expired
approvals, replay, model-supplied authority, scope expansion, and missing
rollback evidence.

Validation evidence:

- `operations/reports/bs0-17-exact-scope-approval-semantics-2026-07-15.md`

### B5.2

The fixture/executor gate path is split across the capability state, readiness,
approval, and fixture-policy layers:

- `projects/brain-core/src/adapters/execution-plans.ts`
- `projects/brain-core/src/adapters/actions.ts`
- `projects/brain-core/src/adapters/continuous-processing-service.ts`
- `projects/brain-core/src/adapters/infinite-brain-proposal-executor.ts`
- `projects/brain-core/src/adapters/infinite-brain-proposal-application-planner.ts`
- `tools/n8n-save-to-mind-fixture-adapter.mjs`

Those layers fail closed before any file write when:

- the relevant capability is not in the required state;
- the execution feature flag is disabled;
- the kill switch is enabled;
- the proposal category or target path is not approved;
- the exact-scope approval is invalid;
- the fixture-only path policy is violated.

Blocked cases preserve fixture hashes because the write step is never reached.

Validation evidence:

- `projects/brain-core/src/tests/execution-kill-switch.test.ts`
- `projects/brain-core/src/tests/continuous-processing-disable-recovery.test.ts`
- `tools/n8n-save-to-mind-fixture-adapter.test.mjs`
- `projects/brain-core/src/tests/infinite-brain-proposal-application-planner.test.ts`

### B5.3

The synthetic fixture loop is exercised by:

- `tools/n8n-save-to-mind-fixture-adapter.mjs`
- `tools/n8n-save-to-mind-topology-plan.mjs`
- `tools/n8n-save-to-mind-plan.mjs`
- `projects/brain-core/src/adapters/infinite-brain-metadata-write-rollback.ts`

The loop preserves the required chain:

proposal -> preview -> approval -> fixture write -> verification -> rollback -> verification

Evidence fields and checks cover:

- before/after/rollback hashes;
- exact approved path;
- receipt fields;
- idempotency;
- unrelated fixture paths unchanged;
- replay rejected;
- rollback restores the original fixture.

Validation evidence:

- `tools/n8n-save-to-mind-fixture-adapter.test.mjs`
- `tools/n8n-save-to-mind-topology-plan.test.mjs`
- `projects/brain-core/src/tests/infinite-brain-metadata-write-rollback.test.ts`

## Provider-admission note

Workbench Private moved to a new committed HEAD with telemetry-only lock
instrumentation in `packages/cli/src/agent/capability-operation-store.ts`. No
admitted tool, approval mode, lease, rollback, audit, receipt, or Brain-admitted
tool scope changed. Brain repinned the provider revision and pinned artifact
digests without modifying Workbench Private.

Current local provider HEAD:

- `5c23708a4fdc4c0e1d871620d6e818b82bc59d28`

Changed Workbench artifacts:

- `packages/cli/src/agent/capability-operation-store.ts`
- `packages/cli/dist/agent/capability-operation-store.js`

## Remaining blockers

- BS0.19 remains blocked until Mind M1.4 task authority is resolved.
- B1.0a remains incomplete.
- B5.4 remains blocked on Mind prerequisites.

## Next independent canonical task

`B6.1 - Add pilot manifest and runner`
