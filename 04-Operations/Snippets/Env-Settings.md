DOMAIN_NAME=prochat.tools               # The top level domain to serve from

SUBDOMAIN=n8n                           # The subdomain to serve from

GENERIC_TIMEZONE=Europe/Lisbon

SSL_EMAIL=info@prochat.tools            # The email address to use for the SSL certificate creation

POSTGRES_USER=maestro                   # Choose a PostgreSQL username
POSTGRES_PASSWORD=duvtiz-cIvry5-mahdoq  # Choose a *strong* password
POSTGRES_DB=n8ndb                       # Choose a database name