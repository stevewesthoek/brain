# Claude configs

This folder is symlinked from ~/.claude.
Everything here is ignored by default (likely contains machine-specific and/or sensitive data).
If we ever want to version specific Claude config files, we will explicitly unignore them.
