{
  "name": "mind-steward",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only mind-steward dry-run planner for Mind OS maintenance loops.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "ci": "npm run typecheck && npm test"
  },
  "dependencies": {
    "tsx": "^4.19.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
