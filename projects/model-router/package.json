{
  "name": "model-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only model-router dry-run planner for Mind OS maintenance loops.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "ci": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
